# Configuration
1. Fixed the initial DataSource error by adding H2 (in-memory DB) for local dev alongside the existing Postgres driver.
2. Converted `application.properties` → `application.yml`.
3. Added `.env` support via `spring.config.import=optional:file:.env[.properties]`, with `.env` gitignored and `.env.example` committed as a template.
4. Created `application-test.yml` and `application-prod.yml` — both point at real Postgres via `${DATASOURCE_URL}/${DATASOURCE_USERNAME}/${DATASOURCE_PASSWORD}`.

# Testing
5. Added Testcontainers (`spring-boot-testcontainers`, `testcontainers-junit-jupiter`, `testcontainers-postgresql`) and wired `UrlshortenerApplicationTests` to spin up a real Postgres container via `@ServiceConnection`. (Note: Docker isn't installed on this machine, so that test can't actually run here yet.)

# H2 Console (dev-only)
6. Enabling `spring.h2.console.enabled` alone wasn't sufficient — took three fixes: added the `spring-boot-h2console dependency` (split out as its own module in Boot 4.x), and added `H2ConsoleSecurityConfig` using `PathRequest.toH2Console()` to permit it through Spring Security while keeping everything else protected. Confirmed working at `/h2-console` (user `sa`, blank password).

# Dev experience
7. Added `spring-boot-devtools` for auto-restart on recompile — verified it works.


- `Link.java` — JPA entity (id, shortCode, originalUrl, createdAt)
- `LinkRepository.java` — Spring Data JPA repo with findByShortCode
- `LinkService.java` — generates a random 7-char base62 short code (retries on collision)
- `CreateLinkRequest` / `LinkResponse` — request/response DTOs, with `@NotBlank` `@URL` validation
- `LinkController.java` — `POST /api/links` (create) and `GET /{shortCode}` (redirect)
- `db/migration/V1__create_links_table.sql` — Flyway migration (used SQL-standard `GENERATED BY DEFAULT AS IDENTITY` so it works unmodified on both H2 and Postgres)